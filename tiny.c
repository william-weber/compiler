int abc;
int gcd (int u, int v)
 { if (v == 0) return u;
    else return gcd(v, u-u/v*v);
    /* dfkljdf */
 }
 
 void main(void)
 {    int x; int y;
    x = main(); y = gcd();
    gcd(gcd(x, y));
 }
int main2(void test, int test[]) {
  int q[12];
}
void main3(void)
{
  int x;
  int y;
  int z;
  float m;
   while(x+3 > 5)
   {
     main3(x);
     x = y + m / z;
     m = x - y + z * m / z;
   }
   return m;
}
int sub(int x)
{
   return(x+x);
}
int main4(int z)
{
  int x;
  int y;
  y = sub(x);
  return x;
}
